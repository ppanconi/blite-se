{
    :: seq
        inv <"s1"> start("john");
        inv <"s1"> corre("john");
    qes,
    
    :: seq
        inv <"s1"> start("bill");
        inv <"s1"> corre("bill");
    qes,
    
    [
        seq
            rcv <"s1"> start(y);
            x := y;
            rcv <"s1"> corre(x)
        qes
    ]
} (x)
